{
  "name": "dice-roller-aa",
  "version": "1.0.0",
  "description": "A REST API for rolling dice",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
    "build": "npm install && npm run generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
